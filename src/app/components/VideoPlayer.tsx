import React from 'react'
import { Box } from "@mui/material";

const VideoPlayer = () => {
    return (
        <Box
          sx={{
            flex: 1,
            backgroundColor: "#6b7280",
            width: "100%",
            minHeight: 0,
          }}
        />
      );
}

export default VideoPlayer